import { MobileNumberFormatterDirective } from './mobile-number-formatter.directive';

describe('MobileNumberFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileNumberFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
